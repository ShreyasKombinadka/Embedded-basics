## Command-driven serial input

A simple system to receive commands over serial, decode them, and perform the required action.

- Uses serial communication to send commands to the microcontroller.
- Implements two basic commands to control LED behavior.
