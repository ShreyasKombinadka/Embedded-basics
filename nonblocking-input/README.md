## **Non-blocking input**

This project exists to understand edge-based input and timing in embedded systems.

- Example code for basic non-blocking input handling and debouncing.
- Uses `millis()` to process input events without halting the processor.

---